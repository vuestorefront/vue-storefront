name: Check licenses
description: Analyses software licenses of dependencies used in the project
inputs:
  projectPath:
    description: Path to start checking dependencies from
    required: true
runs:
  using: 'node16'
  main: 'dist/index.js'