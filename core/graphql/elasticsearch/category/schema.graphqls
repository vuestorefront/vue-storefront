type Category {
  id: Int!
  parent_id: Int
  name: String
  is_active: Int
  position: Int
  level: Int
  product_count: Int
  tsk: Int
}

type Query {
  Category(id: Int!): Category
  CategoryList(query: String!): [Category]
}
