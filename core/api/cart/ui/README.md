# UI Api module for Cart

This module is imported separately since it's relying on the actual implementation which can be different. It contains helpers allowing to develop UI interactions much faster.