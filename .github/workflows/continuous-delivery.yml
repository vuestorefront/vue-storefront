name: VSF Continuous Delivery

on:
  workflow_dispatch:
  push:
    branches:
      - main
      
jobs:
  release-packages:
    name: "Release NPM Packages"
    uses: vuestorefront/vue-storefront/.github/workflows/cd-integrations.yml@no-npm-login
    secrets: inherit
    with:
      enterprise: false
