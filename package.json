{
  "name": "root",
  "private": true,
  "scripts": {
    "build:composables": "cd packages/composables && yarn build",
    "build:core": "cd packages/core && tsc",
    "build": "yarn build:core && yarn build:composables"
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "webpack-bundle-analyzer": "^3.5.2"
  }
}
